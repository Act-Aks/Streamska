import { StyleSheet } from 'react-native'

export const tabBarIndicatorStyles = StyleSheet.create({
    indicator: {
        borderRadius: 2,
        height: 4,
        position: 'absolute',
        top: 0,
    },
})
